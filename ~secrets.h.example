#pragma once

/*
 * Copy this file to ~secrets.h and replace the placeholder with a strong
 * device-specific token. ~secrets.h is ignored by Git and included before
 * ProductConfig.h.
 */
#define FW_OTA_BEARER_TOKEN "replace-with-a-private-random-token"
